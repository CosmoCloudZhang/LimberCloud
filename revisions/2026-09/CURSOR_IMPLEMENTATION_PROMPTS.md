# Implementation handoffs: remote code and local manuscript

Finalized 19 September 2026. These are copy-and-paste implementation prompts for the next stages, not a record of executed changes or authorization to launch every stage at once. The current local directory is the LimberCloud Git checkout; this revision package lives at `revisions/2026-09/`. Commit and synchronize the plans through the code repository before the remote handoff. Paths below refer to this revision package unless they clearly name repository files. Do not assume local macOS paths exist on NERSC.

Required context: [code plan](CODE_REVISION_PLAN.md), [manuscript plan](MANUSCRIPT_REVISION_PLAN.md), [comment inventory](COAUTHOR_COMMENT_INVENTORY.md), [scientific source audit](supporting/limber_ensemble_followup.md), and [environment follow-up](supporting/limber_environment_followup.md). Scientific source review began from `0876bf4a50e869be1289a3eecb46931e7c8eb534`; subsequent local maintenance and manuscript-submodule changes mean that this is a historical audit reference, not the required current HEAD. Record the actual code revision and the paper commit recorded by the parent before each remote stage; record the actual paper checkout revision during local manuscript work. The finalized plans supersede conflicting earlier audit recommendations.

Use Prompts 0, 0A, 1, 2 and 3 in order in remote Cursor on NERSC: inventory, environment/maintenance, science/artifacts, pilot, then the explicitly invoked production stage. Prompt 4 is a separate local Codex manuscript handoff; its editorial work can proceed alongside code, while numerical claims and final figures wait for accepted products. Local Codex owns plans, review, manuscript source, final paper figure/table integration and paper compilation. Remote Cursor owns code, scripts, Python notebook implementation, environment recipes/helpers and scientific testing/execution. Do not edit the same implementation files locally and remotely at the same time. Exchange bounded code changes and reports through Git; record the code commit, recorded paper pin, checks run, external artifact locations, unresolved items and next gate. Do not commit runtime datasets, spectra or secrets.

The manuscript is the separate LimberCloudPaper repository at `manuscript/`, recorded as a submodule by LimberCloud. It is initialized locally and intentionally deinitialized or absent on NERSC. Keep `.gitmodules` and the tracked gitlink intact; do not initialize the paper, run `git -C manuscript`, edit TeX or write into paper figures/manifests on NERSC. A Git command in an ordinary leftover directory can silently inspect the parent repository. Remote agents obtain the recorded paper pin with `git ls-tree HEAD manuscript` and use the manuscript plan/comment inventory as their paper context. An unavailable paper detail should be referred to the local owner without initializing the paper remotely.

For clean NERSC checkouts, synchronize code with `git pull --ff-only --no-recurse-submodules`; do not append a submodule initialization command. Locally, synchronize the parent and then use `git submodule update --init --recursive manuscript`, inspecting both working trees and switching to the intended paper branch before editing. A parent commit does not commit paper edits. When publication to GitHub is requested, push the paper commit before the parent commit that records its pointer. Stage only reviewed task paths and inspect staged diffs; do not blindly stage unrelated changes. Do not introduce another outer workspace, convert the paper back to ordinary tracked files, or rewrite old history. Refresh documentation under the already-renamed `documents/` directory, including the different local/remote workflows and direct paper-repository/Overleaf instructions.

Remote figure generation must export accepted publication products to a dedicated CFS output directory, not `manuscript/`. The handoff contains only validated figure files, compact tables/summaries and a manifest with checksums, producing code commit, run/sample/configuration/covariance identities, tested environment and acceptance evidence. Large arrays remain on CFS. The local owner transfers the selected products, verifies the manifest/checksums, integrates them into the paper and updates the paper's figure/table provenance. This division applies to every remote prompt below.

A stage ending at a documented validation gate is useful; do not repeatedly ask permission for routine edits/tests already authorized by its prompt. Ask for genuinely missing scientific choices only where repository provenance and author instructions cannot resolve them, and continue independent work. Existing production drivers currently hard-code 1,000 iterations: `Single` selects a probe configuration and `--number` labels CPU allocation. Neither is a small-run control. Do not execute them as smoke tests before implementing and checking explicit sample limits.

## Prompt 0 — Remote Cursor: read-only NERSC inventory and execution handoff

```text
Prepare the first remote handoff in the actual NERSC checkout. Read revisions/2026-09/CODE_REVISION_PLAN.md, especially C00/C13/C15/C16, MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md, the environment follow-up, and the current launcher/environment documentation. This prompt authorizes inventory and a written handoff report only, not environment replacement, scientific production jobs or source implementation. Manuscript editing belongs to local Codex; its working directory is intentionally absent or deinitialized here.

Record repository revision/branch/status and local instructions. Record the parent's paper pin using git ls-tree HEAD manuscript. Do not initialize the manuscript, run git -C manuscript or require a live paper checkout revision. Keep the tracked gitlink and .gitmodules intact. Identify the actual PSCRATCH checkout, CFS runtime datasets/config/results/log locations, a CFS destination for publication-product handoffs, and external OneCovariance checkout/version. Read configuration metadata, file schemas/shapes, representative labels and manageable hashes needed to establish provenance, without dumping large data or secrets.

Inventory the working CosmoConda environment, .venv link/interpreter, current notebook kernels, Python/dependency versions, activation hooks, relevant NERSC modules, CPU/GPU profiles and configured scheduler resources. Distinguish installed Python libraries from loaded MPI/HDF5 system modules. Read current official NERSC mpi4py and h5py instructions linked in the environment follow-up; identify the candidate MPI/HDF5 build and compiler stack without claiming runtime compatibility from metadata alone. Use package/build metadata and extension-library linkage for this read-only inventory. Do not import mpi4py.MPI, call MPI.Get_library_version(), or import potentially MPI-initializing h5py on a login host to obtain capability information. Defer those runtime checks to an existing supported allocation or Prompt 0A, and explicitly mark unverified MPI/HDF5 capabilities. Do not install a generic MPI stack over NERSC's configured stack.

Determine which configuration variables, override paths and legacy aliases have actual external callers. In particular audit LIMBERCLOUD_CONDA_ENV, LIMBERCLOUD_ENV_FILE and LIMBERCLOUD_REPO_ROOT before their coordinated replacement. PROJECT_ROOT will be the automatically discovered code root, .venv the sole selected Python prefix, and RUNTIME_ROOT will remain external data/results storage. Inspect copied-SLURM-script root discovery. Review how root discovery handles a nested paper repository for local use, without initializing that repository on NERSC.

Confirm that the current runner's --number is a host CPU allocation label, not a sample limit, and identify where to add explicit --sample-count/--fiducial-only validation controls before any computational smoke run. Report dependency/module mismatches, available lightweight software checks and exact allocation-based tests needed next. Produce the inventory report and a bounded next-stage proposal with actual paths, versions, code commit, recorded paper pin and unresolved inputs; do not run the 1,000-iteration legacy drivers, create a new environment or submit jobs in this inventory stage.
```

## Prompt 0A — Remote Cursor: dedicated environment, simpler launchers and maintenance

```text
Implement the accepted environment and maintenance stage remotely after Prompt 0. Read CODE_REVISION_PLAN.md C00/C13/C15/C16, MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md and supporting/limber_environment_followup.md, reconciling all choices with the recorded NERSC inventory. The manuscript stays deinitialized/absent: record its pin with git ls-tree HEAD manuscript, and do not initialize it, run git -C manuscript or edit its files. Preserve the working CosmoConda environment while creating and validating a separate minimal limbercloud environment. Exclude CosmoSIS. Include the scientific/plotting/kernel/development dependencies from the plan, explicit CAMB, h5py for current spectra persistence and mpi4py for future task-based analyses. Symbolic Mathematica derivations and external LaTeX are separate tools; do not add SymPy without a new actual use. Audit OneCovariance's own requirements at its actual installed revision.

Provide a portable CPU recipe and a NERSC CUDA variant with compatible scientific versions; do not put a mandatory CUDA JAX install in the laptop recipe. Follow the current official NERSC MPI/h5py installation procedure for the chosen stack rather than assuming an ordinary Conda/pip MPI binary is compatible. Record and validate MPI/compiler/HDF5/Conda activation identities. Installing mpi4py does not authorize changing the paper's single-task benchmark into an MPI campaign. The NERSC recipe may build MPI-enabled h5py as instructed by NERSC, while the portable recipe can use serial h5py; current output still uses one serial writer per artifact with no mpio driver. Future parallel file access must be explicit and separately validated. Apply current NERSC file-locking guidance to the actual CFS profile before importing h5py, record the setting and verify file round trips there. Disabling HDF5 locks requires independent exclusive run ownership and does not make concurrent writers safe.

Unify notebook, editor, script and batch Python selection through the environment prefix referenced by .venv. Batch launchers must retain necessary Conda activation hooks and NERSC modules for that same prefix. Use a fixed PROJECT_ROOT/.env for external path configuration. Remove redundant LIMBERCLOUD_CONDA_ENV, LIMBERCLOUD_ENV_FILE and exported LIMBERCLOUD_REPO_ROOT selection, and obsolete legacy aliases, by updating their repository and verified external callers together. PROJECT_ROOT is internal and automatically discovered; RUNTIME_ROOT remains separate. Keep LIMBERCLOUD_RUNTIME_ROOT, conditional LIMBERCLOUD_ONECOVARIANCE_ROOT and optional LIMBERCLOUD_TEXLIVE_BIN for actual external resources; optional settings should be commented examples. Rename the config-only load_environment.sh helper to load_config.sh at the agreed location and update all callers, tests and documentation. Preserve the non-executing configuration parser and documented exported-value precedence. Make shared configuration loading work with supported macOS shells without breaking NERSC Bash, including copied SLURM scripts and code-root discovery when invoked near the paper submodule.

Apply the agreed style and path cleanup in bounded patches. Use Ruff import ordering while preserving JAX initialization/device setup side effects. Use Google-style Args/Returns sections with useful types, shapes and units; the summary sentence starts on the line after the opening triple quotes, and closing triple quotes occupy their own line. Keep blank lines whitespace-free. Enable editor indentation guides and active indentation highlighting. Add verified scientific names to the installed spellchecker's user dictionary only within the authorized settings scope; fix typos and do not blindly accept every unknown word or commit personal settings. Remove the 26 verified unused path parameters (24 spectra runners and two benchmark readers), the obsolete --path CLI plumbing and their shell callers together; preserve the meaningful folder/runtime argument and scientific behavior. Audit again if these counts change with current HEAD.

Make the user workflow understandable through short commands/documentation for selecting the environment, checking it and registering a kernel; keep environment creation separate from ordinary launch. Update README and documents/ links for the actual directory structure and manuscript submodule, preserving documentation changes already synchronized from local planning. Explain local paper ownership and NERSC's intentionally absent paper, including git pull --ff-only --no-recurse-submodules on NERSC. Replace any remaining obsolete parent-repository subtree publishing instructions with the direct paper-repository workflow. Keep code checks independent of an initialized manuscript; make local manuscript-building prerequisites clear. Verify package distributions exclude paper/revision assets as intended without initializing the paper or rewriting repository history. Use temporary synthetic nested-repository fixtures for root-discovery tests where needed. Report macOS checks requiring local review without assigning simultaneous source edits to the local owner.

Before invoking any science runner, add and test explicit --sample-count and --fiducial-only controls with safe validation defaults; sample-count counts nonfiducial rows, fiducial-only implies zero sampled rows, and inconsistent selections must fail. The campaign must explicitly request 1,000 sampled rows plus the fiducial. Existing Single and --number arguments do not provide this guard. Use bounded software checks and tiny allocated environment/kernel/backend/MPI compatibility smoke tests according to the accepted inventory, never production workloads on login nodes. A two-rank MPI compatibility test, if used, validates the dependency only and is separate from single-task benchmark results. Preserve the old environment until the dedicated one and notebook launches pass. Report changes, exact tested versions/modules/interpreters, test evidence, code commit, recorded paper pin and checks still pending on either platform. Do not launch the 1,001-row campaign.
```

## Prompt 1 — Remote Cursor: scientific contract, shared samples and artifact foundation

```text
Implement the science/artifact stage remotely after the accepted inventory and environment/maintenance stage in this actual checkout. Read revisions/2026-09/CODE_REVISION_PLAN.md C01–C09/C14, MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md and supporting/limber_ensemble_followup.md before editing. Inspect git status/revision, local instructions, the validated environment helpers and external runtime paths; preserve uncommitted work and reconcile differences from historical scientific audit commit 0876bf4a50e869be1289a3eecb46931e7c8eb534. Record the paper pin using git ls-tree HEAD manuscript. Do not initialize the paper, run git -C manuscript, or edit manuscript files; use the plans/comment inventory as paper context and refer missing paper details to the local owner. Do not reset the checkout or recreate a working environment. Work in small reviewable patches and record scientific choices, current revision and validation evidence. Confirm explicit tiny-run controls work before executing a scientific driver.

Implement a shared evaluation contract and one canonical seeded cosmology-table generator. The paper campaign is fiducial sample 0 plus 1,000 sampled IDs 1–1000, shared across Y1/Y10, all configurations, all backends and all NUMERIC orders. Persist actual values, parameter order/bounds, seed/RNG metadata and hash. The fiducial consumes no random draw. Existing drivers are unseeded and disagree between ±5% and ±10%; reconcile the domain explicitly (proposed default ±10% for current nonzero parameters), sort bounds for negative w0, and keep wa/curvature fixed where currently zero. Resume by sample ID, never by re-drawing. Preserve Single=EE, Double=TE+TT, Triple=EE+TE+TT.

Resolve scientific equivalence before sharing spectra: named CCL magnification slope s versus analytical q=5s−2; missing MS response; sampled H/Omega_m prefactors and distance conversion; declared IA/bias functions and whether they vary with cosmology; consistent final EE/TE/TT assembly (currently JAX leaves components separate); identical ell coordinates/estimators. Current experiments compare 20 CCL centres with 21 LimberCloud nodes; notebooks separately mix centres with band averages. Preserve raw sampled spectra where covariance preparation needs them. Resolve the generator eta_IA=0.5 versus manuscript eta_IA=0 discrepancy from author-approved provenance; if unresolved, expose it as an explicit scientific decision without inventing a value.

Do not implement the old recommendation to change the NN observer coefficient from 1/4 to 1/2. The current first interval uses P=P1*(chi/chi1)^3; the NN factors 1/12,1/12,1/4 are consistent with that convention. Test first/final interval contracts independently across the structural cases. Existing NUMERIC-linear interpolates a(chi), whereas the analytic implementation linearizes 1+z and has the special power interval; it is not automatically quadrature of the identical integrand. Provide a small independent matched-integrand quadrature oracle and preserve the distinct approximation-comparison role of NUMERIC.

Implement the C09 schema/path helpers and atomic sample checkpoints, using existing family/device/survey roots with new run-ID subdirectories. Use h5py/HDF5 for spectra and retain the small canonical Cosmologies.npz table plus JSON provenance. Store final spectra, coordinates/pair labels, sample identity, per-sample stage durations and completion/failure metadata; never retain all cosmologies' coefficient tensors. Enforce one writer/resumer per artifact namespace. Use per-sample HDF5 checkpoints written to temporary files then closed/validated/renamed on the destination filesystem, checksums, and a manifest published last. Keep durable products/checkpoints on CFS; PSCRATCH staging requires a verified copy to a temporary CFS file before publication because a cross-filesystem move is not atomic. Restart must validate sample/configuration/schema identity and detect interrupted/incomplete outputs; HDF5 itself does not make in-place writes transactional. Consolidation into final .h5 products must also be validated before publication; readers consume completed manifests only. Base spectra must be reusable before covariance exists. Preserve meaningful old CLI argument semantics through wrappers and provide explicit legacy-reading mode, while retaining the accepted removal of the unused --path plumbing. Add focused tests for shared samples/restart, HDF5 shape/axis/provenance and interrupted-write handling, known component defects, endpoint formulas, matching axes and artifact rejection. Run applicable fast checks and report evidence plus unresolved science choices. Do not submit the 1,001-row production campaign in this stage.
```

## Prompt 2 — Remote Cursor: NUMERIC experiments, saved spectra and sequential timing

```text
Continue remotely from the accepted science/sample/artifact foundation. Read CODE_REVISION_PLAN.md C08–C10/C12–C14, MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md and the fresh audit. Record the code revision and paper pin (git ls-tree HEAD manuscript). Keep the paper deinitialized/absent; do not run git -C manuscript or write to manuscript/. Implement shared sequential experiment execution for the existing 24 Python entries and one NUMERIC family with --interpolation linear|quadratic|cubic. Map linear to the legacy slinear interpolant and document which radial functions each setting interpolates. Extract and reproduce the notebook reference routines first; then accept tolerance/estimator corrections with distinct identities and focused tests. Cubic is a comparator, not assumed exact truth. No comprehensive optimal-z/ell-grid or new power-law development is requested.

Keep one SLURM task, srun -n 1 and the sequential cosmology loop. Preserve existing internal Numba threading and JAX compiled/vectorized/device execution. Do not add multiprocessing, MPI execution, sample job arrays, distributed scheduling or a task-throughput study; mpi4py is installed for future work only. Existing Run_All.sh can submit independent method/survey/configuration jobs with declared uncontended resources. Reuse the accepted load_config.sh helper, .venv-selected environment and validated CPU/GPU profiles. Record actual affinity, thread pools and device state; --number is the host CPU-allocation label even for GPU jobs, not a GPU count or sample count.

Implement the exact C09 naming convention through one helper. Examples:
  results/spectra/NUMBA/Y1/<run_id>/Time_Triple_128.txt
  results/spectra/NUMBA/Y1/<run_id>/Spectra_Triple_128_EE.h5
  results/spectra/NUMERIC/LINEAR/Y1/<run_id>/Time_Triple_128_LINEAR.txt
  results/spectra/NUMERIC/LINEAR/Y1/<run_id>/Spectra_Triple_128_LINEAR_EE.h5
Use corresponding unchanged timing stage suffixes, TE/TT, other configurations, Y10 and QUADRATIC/CUBIC. Preserve JAX/CPU and JAX/GPU directories. Save nonoverlapping per-sample stages once in Time_<configuration>_<allocation>[_<order>]_SAMPLES.h5 and link it through the manifest, retaining existing cumulative TXT basenames/stages. Consolidated HDF5 spectra use float64 cl axes (sample, ell, pair) and contain 1,001 rows with fiducial first, but all readers identify it by sample ID/is_fiducial. Keep raw covariance input and derived bandpowers in distinct named datasets with their own coordinates/estimator metadata. Choose dataset chunking for bounded sample/ell reads, record lossless compression if used without an undeclared reader plugin dependency, and keep all output I/O outside compute timers. Preserve the single-writer checkpoint/publication contract from C09; no collective mpio writer is needed. Update both benchmark readers and ProjectPaths explicitly; old exact-path readers do not automatically support run-ID directories or NUMERIC.

Keep cold startup/fiducial/compile costs separate. Time the 1,000 sampled evaluations with perf_counter and existing JAX synchronization, including identical final component assembly and a declared transfer/estimator boundary. Derive cumulative timings at 100,200,...,1000 by summing nonoverlapping per-sample compute durations. Save/checkpoint outside compute timers and separately record total job wall time including I/O. Record CCL lazy initialization semantics rather than claiming stage equivalence from names. Timed Single/Double/Triple must execute their own workload, not slice cached Triple output. Resume science sequentially; label resumed timing sums as segmented, with warm-up per segment outside timers. A continuous benchmark claim needs an uninterrupted accepted run.

Make the six spectra notebooks load the saved fiducial products without expensive evaluator imports/calls. Read only the requested HDF5 slices, close file handles and figures, bound figure dimensions/DPI and retain explanatory markdown. Configure generated figures to use runtime/CFS output paths, never a required manuscript directory; publication export and local paper integration are separate steps. Run focused software checks, then use a small allocated pilot with explicit validated --sample-count/--fiducial-only limits and the actual configured account/resources to validate each method/order, reference tolerance, memory and rough campaign duration. If an allocation is unavailable, prepare the exact launch commands and record that limitation; do not run expensive work on login nodes. The pilot report must distinguish code checks from scientific acceptance and estimate the remaining campaign cost. Prepare the full campaign launchers; production execution belongs to the next explicitly invoked and accepted stage.
```

## Prompt 3 — Remote Cursor: correct covariance, ensemble diagnostics and final campaign

```text
Continue remotely from the accepted pilot, reading CODE_REVISION_PLAN.md C05–C07/C11–C14, MANUSCRIPT_REVISION_PLAN.md evidence gates and COAUTHOR_COMMENT_INVENTORY.md. Record the code revision and parent's paper pin using git ls-tree HEAD manuscript. Keep the paper deinitialized/absent, never run git -C manuscript, and leave paper source/figure manifests to the local owner. Reconcile the installed OneCovariance revision/configuration with the pinned historical audit before reusing interface assumptions. Implement explicit ell-major/bin-i/bin-j input serialization, full required pairs, float64 and separate source/lens grids. Implement explicit covariance output labels/pair maps; fix wrong TT/EE triangle mapping. Verify with input/output sentinels and an independent Gaussian observed-field covariance calculation. Record noise, window, component and analysis-mask identities. Do not use jitter, eigenvalue clipping or a positive-definite repair to hide a contract defect.

Use one validated fiducial covariance per survey with a fixed selected vector for the main ensemble. Factor it once. No 1,000-covariance campaign is required. Keep all necessary fields for covariance construction; apply the same selection to differences and both covariance axes. Implement the documented lens/source eligibility and scale-cut rules, validating their actual selected pair list. Keep all-pair diagnostics distinguishable from science-selected results.

Run the accepted fiducial-plus-1,000 shared-table campaign for CCL, NUMBA, JAX CPU/GPU, and NUMERIC LINEAR/QUADRATIC/CUBIC, across the requested Y1/Y10 and Single/Double/Triple configurations, using one sequential task per configuration and current approved NERSC account/resources. Timing-mode runs may supply spectra for accuracy summaries, so do not duplicate the full calculation unnecessarily. Preserve completed sample checkpoints on failures, report attempted/completed/matched counts, and never silently replace or drop failed samples. If wall-time constraints force segmented accumulation, label that evidence correctly; obtain continuous timing evidence before making uninterrupted-run claims.

Make all error notebooks load saved data/summaries. Preserve their main panel layout and absolute fractional/absolute covariance-scaled residuals on log y axes. Use each sample's CCL spectrum as its denominator. Keep fiducial curves; optionally show median and pointwise 16th–84th bands computed after taking absolute values of each sampled residual. Exclude the fiducial from sampled quantiles. Label bands as variation over the sampled cosmologies. Preserve signed residuals for diagnosis. Undefined near-zero-reference ratios are masked and counted; exact residual zeros stay zero in raw products/statistics and receive an explicit log-display convention only.

Compute D_s = delta C_s^T Sigma_fid^{-1} delta C_s by a stable solve for each cosmology. Report full D primarily and optional D/N_data; explain that this deterministic discrepancy has ideal value zero, not the noisy-data reduced-chi-square expectation near one. It is not the arbitrary-data likelihood shift. Do not sum per-probe quadratic forms to claim joint D or sum the 1,000 cosmologies as independent survey observations. Save the fixed vector/covariance identity and all per-sample values.

Generate Y1/Y10 tables with fiducial D, sampled median and 16th–84th range, successful counts, N_data and tail/worst-sample diagnostics. Decide whether an ECDF, parameter scatter or violin adds useful information only after inspecting the measured distribution. Regenerate compact figures in clean plotting kernels. Export accepted publication figures and compact tables/summaries to the dedicated CFS handoff directory with a manifest containing checksums, producing code commit, sample/run/configuration/covariance identities, environment and acceptance evidence. Leave full spectra/covariances on CFS; avoid unpublished source changes in producing-code provenance, or explicitly identify the patch if unavoidable. The local owner transfers selected accepted products, verifies their manifest and updates paper figures/tables/provenance. Return validation reports, campaign counts/timing semantics, artifact locations, the recorded paper pin and any unresolved scientific limitations. Do not fabricate missing numerical evidence or edit manuscript files or claims remotely.
```

## Prompt 4 — Local Codex: manuscript structure and coauthor responses

Run this prompt in the local LimberCloud checkout with its initialized paper submodule. Do not send it to the NERSC agent. Its destination is deliberately different from Prompts 0–3; the filename is retained so existing links remain valid.

```text
Revise the LimberCloud manuscript locally using revisions/2026-09/MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md, CODE_REVISION_PLAN.md and supporting/limber_ensemble_followup.md. The manuscript is the independent LimberCloudPaper Git repository at manuscript/, initialized in this local checkout; its absence on NERSC is intentional. Confirm the local paper is an actual initialized repository before using git -C manuscript, reconcile parent and paper revisions/status, and preserve author information/unrelated changes. Work on the intended paper branch rather than accidentally committing from a detached submodule checkout. Code/scripts/notebook/environment implementation belongs to remote Cursor on NERSC; provide review findings through a bounded handoff rather than editing those same files concurrently. Keep all 102 source comment IDs in a response ledger, linking each implemented edit to its location and any required evidence. Author clarifications in the finalized 19 September plans supersede conflicting provisional interpretations; do not rewrite the original coauthor feedback or attribute new author choices to a coauthor.

Start with editorial changes that do not need new numerical results. Add the GitHub link at first appropriate mention and a code-availability statement. Show the central tensor-contraction equation, with defined symbols and assumptions, at the end of the Introduction or start of Section 2 before the detailed derivation. Follow the planned compact derivation/validation structure and preserve LaTeX labels through the initial move, keeping a crosswalk. Explain what is reusable and what changes with cosmology.

Explain why NUMERIC linear/quadratic/cubic comparisons are included: they test whether the adopted linear representations are adequate for the kernels/effective power in this application. State which functions/coordinates each setting interpolates; cubic is a useful conventional comparator, not guaranteed truth. Distinguish same-integrand quadrature tests from changed-representation comparisons. Distribution factorization requires a basis linear in the node coefficients; do not claim only piecewise linear interpolation can factor out those coefficients. The present basis is chosen for local support and simple coefficients. Current ordinary-interval power interpolation is linear along the Limber trajectory, with a special cubic observer interval. Alternative power functions can supply nodes; alternate continuous power-law representations require new coefficient derivation/validation and are future work.

Describe Delta z=0.01 as the adopted accuracy/cost choice, not a necessary or proven optimal grid. Coarser/finer grids are possible; tensor cost grows with node count, and refinement can improve representation accuracy within the full numerical error budget. State the intended SRD provenance of ell binning after verifying the actual source and edges. Other sampling/windows are possible, and smooth spectra motivate future sparse/interpolated strategies. Do not promise an optimal radial/ell grid or require a comprehensive optimisation study to finish this paper.

Describe one fiducial plus 1,000 sampled cosmologies, a saved common sample table, all methods/orders, actual sampling ranges and a one-task sequential benchmark with existing internal parallelism. Preserve Single=EE, Double=TE+TT, Triple=EE+TE+TT. Put task-level parallelism in future real-analysis work: the mpi4py dependency does not establish a measured MPI capability or scaling result. Explain saved HDF5 spectra and lightweight plotting as reproducibility workflow where appropriate, without exposing incidental environment-variable plumbing in the scientific narrative. Define cold/warm/stage/full timing, exclude checkpoint I/O from compute durations, and exclude the fiducial from sampled cumulative counts.

Preserve main absolute-error/log-axis layouts. Use accepted ensemble summaries for optional pointwise 68% bands, with accurate near-zero handling and empirical-variation wording. Explain fixed-fiducial-covariance D_s and optional D_s/N_data without a fitted/noisy-data chi-square interpretation. Start with a compact ensemble table; select any extra distribution visualization based on actual spread. Do not insert guessed values or claim new accuracy/speed-up results before the code/covariance/campaign gates pass. Keep exact algebra, approximate Gaussian marginalisation and prospective emulation clearly distinct as the detailed plan specifies.

After the remote validation gates pass, obtain the selected accepted publication figures and compact tables/summaries from the CFS handoff directory. Verify transferred files against the remote manifest/checksums and acceptance report before integrating them into the local paper. Do not transfer full spectra/covariances into the paper repository or assume the local machine can access CFS directly; use the established transfer channel or request the accepted bundle when it is unavailable. Update related claims together in captions, tables, abstract and conclusion. Repair producer-path/figure-manifest references; record the producing code commit, run/sample/configuration/covariance identities, product checksums and relevant environment evidence in the paper's provenance. The parent code repository records the chosen paper revision, so do not require circular mutual commit hashes. Compile the manuscript locally and inspect all pages for layout, equations, labels and references; if local TeX is unavailable, report the missing prerequisite and prepare the local setup rather than initializing the manuscript on NERSC. Return the source diff, comment ledger, evidence-linked figure/table manifest, code/paper revisions and remaining unresolved items. Keep code and paper commits separate; when a GitHub push is requested, review and stage only relevant paths, publish the paper commit first and then update/publish the parent submodule pointer. If separately requested, synchronize Overleaf directly from the paper repository using the accepted workflow, not a parent-repository subtree export. No coauthor emails, Overleaf synchronization or external publication are authorized by this editing prompt itself.
```
