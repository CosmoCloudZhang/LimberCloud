# Phase 3 — Covariance, selected vectors, and allocated acceptance pilots

Run this prompt in the remote NERSC code checkout after Phase 2 is accepted. This stage implements and validates covariance and measures readiness for Phase 4. It does not start the full 1,000-sample campaign or edit the paper.

```text
Implement Phase 3 of the revised LimberCloud plan. Read the current CODE_REVISION_PLAN.md, MANUSCRIPT_REVISION_PLAN.md, accepted Phase 1/2 reports and artifact contracts, and supporting covariance/selection audits. Reconcile the working tree against baseline 13a3c2deb3e72bd22cfad5f4eb1a6191899a47d6 and the accepted phase commits. Preserve unrelated changes. Record start commit, branch, status, resolved runtime roots, installed packages, and manuscript gitlink with `git ls-tree HEAD manuscript`. The paper may be absent/deinitialized; do not initialize it or run `git -C manuscript`. Keep scientific outputs on the configured external runtime tree. Do not automatically commit, push, merge, or publish unless separately authorized.

Deliver working code, deterministic tests, actual allocated pilots when resources are available, a covariance acceptance report, and a costed Phase 4 campaign manifest. Fix failures within this scope; do not turn skipped checks into passes. If access/allocation or an unsupported external covariance component blocks a dependent scientific claim, complete the independent code, tests, launchers, diagnostics and export preparation and identify the precise remaining evidence.

Preserve entry/exit source manifests and reconstructable snapshots under the master plan's review-packet contract, and return an incremental phase diff even when no new commit separates phases. Keep reports outside compute fingerprints; retain both source and report provenance without capturing secrets/runtime data/paper contents.

Require the accepted expanded Phase 1 closeout, carried into Phase 2 execution, to resolve the terminal cancellation/observer/zero-power and nuisance-identity findings, with retained executable coverage and allocated compiled-backend results. Run the outstanding real kernel/HDF5 check and record macOS/GPU evidence separately. Synthetic OneCovariance import discovery is not its actual installed-reader acceptance. Timing pilots must verify separate Fiducial/Cosmology products, recorded checkpoint counts (including tiny pilots), the legend label `CCL` with an end-to-end reference caption, and compatible completed generations; fixed canonical names alone are insufficient provenance.

1. Preserve the fixed science and angular contracts.

Every method evaluates the SAME 21 float64 e=geomspace(20,2000,21) nodes: CCL, NUMBA, JAX/CPU, JAX/GPU, and NUMERIC LINEAR/QUADRATIC/CUBIC. Phase 2 outputs contain raw 21-node spectra for reproducibility and derived 20-band spectra for ALL comparison/covariance vectors. The canonical angular reconstruction is s(x)=CubicSpline(log(e),e*C_nodes,bc_type='natural'), C_tilde(ell)=s(log(ell))/ell, B_b=integral(s(x),log(e_b),log(e_(b+1)))/deltaell_b. Import the shared implementation; do not write another approximately matching operator. Include natural boundary condition and operator version in fingerprints. Geometric centres are plotting coordinates, not additional CCL evaluations. No separate 101-node shared spectrum grid is permitted. Radial interpolation order belongs only to NUMERIC and is independent of this angular spline.

Keep eta_IA=0.0 and the accepted physical conventions, sampled-cosmology prefactors, magnification response and endpoint corrections from Phase 1. eta zero does not remove intrinsic alignment. Do not reopen the NN observer 1/4 or final diagonal decisions. Reject old products whose science/window identities differ; do not relabel them.

2. Implement covariance preparation and explicit upstream adapters.

Add or complete src/limbercloud/io/covariance.py, focused modules under src/limbercloud/validation/ for covariance/window/vector handling, experiments/covariance/prepare.py and validate.py, a versioned configuration template/renderer, and tests. Equivalent existing accepted modules may be reused. Convert experiments/covariance/{Y1,Y10}/matrix.py into thin compatible entry points; update both matrix.sh launchers to fail fast on prepare/run/validate failures and use run-ID output directories. Remove duplicated historical tracer construction, overwritten source/lens redshift axes, float32 tables, and unvalidated external CONFIG.ini dependence.

Preparation consumes completed accepted fiducial CCL artifacts and declared survey/noise inputs. Verify configuration, sample identity, raw 21-node and derived 20-band shapes, pair maps and checksums. Preserve each redshift array with its own values. Never use 20 bandpowers as a raw-C_ell table or evaluate a new independent 101-point reference grid. If an upstream reader needs denser interpolation support, generate its internal support by evaluating the canonical 21-node spline, record this as derived adapter/quadrature metadata, and verify the resulting reconstructed mean reproduces the canonical 20 bandpowers within declared tolerance.

Write every Cartesian EE/TT input pair, including both triangles, and all rectangular TE pairs with lens as first index. Serialize deterministically ell-major, first-bin-major, second-bin-major; use float64 and sufficient ASCII digits. Validate unique complete tuples, finite coordinates/values, monotonic ell, expected dimensions, labels and EE/TT symmetry. Preserve signed cross spectra. Use a 2x3 non-square sentinel with distinct ell/pair values, then round-trip through the actual installed OneCovariance reader and compare EVERY decoded value to the source. A self-round-trip through our own serializer is insufficient.

Record the installed OneCovariance commit, source status, effective config, adapter version, input hashes, output labels and numerical settings. Inspect that revision before using private methods. Do not silently edit its external checkout; required upstream changes need a reproducible versioned patch or pin recorded in the artifact. Historical audit pin 91e139622eba568d7f742a2d12f6b66a106e6e68 is evidence, not the installed-version contract.

3. Make the physical covariance match uniform-dell windows.

Use w_b(ell)=1/deltaell_b inside the exact floating bin and zero outside. These are the windows for means AND every covariance component. Raw 21-node values are samples of a theoretical mean, not 21 independent observed modes: do not invent a diagonal covariance on those nodes and project it with the spline matrix.

For the declared continuum full-sky-fsky Gaussian approximation, S_ab(ell)=C_tilde_ab(ell)+N_ab and

Sigma[(ab,b),(cd,b')]=integral w_b*w_b'*(S_ac*S_bd+S_ad*S_bc)/[(2*ell+1)*f_sky] d ell.

Split integration at exact band edges. Disjoint top hats give zero Gaussian cross-band entries in this geometry approximation; cross-pair/probe entries remain. Internal quadrature refinement is allowed and must converge without changing the common 21-node or 20-band data contract. State any geometry approximation explicitly. A flat-sky 2*ell prefactor or discrete integer-mode sum is different; compare matched formulas or prefactor-free contractions, never assert equality of unequal conventions.

At the historical OneCovariance pin, ell-space Gaussian binning uses integer-rounded boundaries and equal integer-mode sums, whereas NG/SSC binning integrates ell*ell' with annulus-area normalization. Merely matching bin count/range does not make its outputs compatible. Implement an explicit project-owned adapter that projects unbinned upstream kernels through w_b on both axes, or a verified arbitrary-summary filter route. Under that pin's (1/2pi)*integral ell*C*W measure, the candidate Fourier filter is W_b=2*pi/(ell*deltaell_b) within each bin. That route also needs consistent real-space filters/pure-noise handling; Fourier-filter matching alone is not acceptance. Test actual installed behavior. Do not silently fall back to default binned output, rebin an incompatible covariance only by centre interpolation, or call integer-rounded bands the author's bands.

Retain a small independent Gaussian reference using separate numerical integration/contraction logic. A project-owned uniform-window Gaussian implementation may supply a transparently identified production covariance if the matched upstream route is unavailable; keep that reference independent of the production implementation, record the production backend truthfully, and leave the OneCovariance-specific acceptance outstanding. Cover TT, TE, EE and all cross-probe contractions, including negative signal cross terms. Check a positive-semidefinite field-spectrum fixture and a noise-only analytic integral: the constant Wick-product numerator multiplies log((2hi+1)/(2lo+1))/(2*f_sky*deltaell^2). Check nonconstant reconstructed signal with independent quadrature, and verify mean-window identity. Never normalize the spline operator artificially: constant C is not exactly reproduced by a natural spline of ell*C versus logell. Exact transformed constant/linear tests and direct integration of the specified spline are appropriate.

Make noise explicit: per-bin densities and units, conversion arcmin^-2 to sr^-1, per-component ellipticity dispersion, same-field/bin noise terms, population-overlap assumptions and survey footprint. Keep theory means noise-free and avoid double-adding noise in upstream code. Preserve the accepted survey area rather than silently replacing it while claiming SRD equivalence.

For independently sampled disjoint catalogues, n_sr=n_arcmin^-2*(180*60/pi)^2, N_TiTj=delta_ij/n_lens_i, and N_EiEj=delta_ij*sigma_e,component_i^2/n_source_i. The manuscript's per-component sigma_e=0.26 has no extra factor 1/2. N_TE=0 requires the stated uncorrelated count/shape-noise assumption; an explicitly different overlap/noise model needs its own declared terms. Require positive densities, nonnegative dispersions and 0<f_sky<=1. Test unit conversion and actual survey amplitudes independently of the constant-Wick-product integration reference. Add physical white noise after reconstructing the noise-free signal, not as node samples passed through the angular spline.

Persist a versioned float64 covariance artifact containing exact input and fiducial identities, full and selected vector labels, selected indices, exact ell edges and plotting coordinates, window/geometry identity, mean bandpowers, noise inputs/units, covariance component identities, upstream/adapter versions, effective configuration, quadrature convergence and validation report links. Publish its completed manifest only after validation; fresh output paths must prevent reuse of stale MATRIX.ascii. One accepted fixed fiducial covariance per survey supplies the Phase 4 comparison scale; do not schedule 1,000 covariance runs.

Gaussian acceptance precedes NG/SSC. For each additional component verify cosmology, tracer/redshift weights, galaxy bias, IA law/sign, magnification convention, response/trispectrum model, angular windows and convergence. Full observed C_ell inputs do not establish that upstream internal NG/SSC contains the same IA/magnification physics. If that physical coverage is unavailable, save a precise limitation, accepted Gaussian evidence and the pending total-covariance gate. Do not publish a physically unmatched total as validated or describe Gaussian-only error scales as a completed full forecast.

4. Define output order and a reproducible selected vector.

Use explicit labels: TT row-major upper triangle, TE row-major lens x source, EE row-major upper triangle, with 20 bands fastest inside each pair. Map actual upstream labels into this order. Implement one label-aware vector adapter: stored per-sample bandpowers are (band,pair), whereas the covariance vector is pair-major with band fastest, so transpose to (pair,band) before flattening each probe and concatenate TT,TE,EE in the declared order. Test a multi-pair,multi-band sentinel end to end; a direct flatten of the stored array is wrong. Test at least three bins, because a two-bin fixture cannot catch the historical TT/EE triangle bug. Full unmasked dimensions are 1100 for five lens/five source bins and 2400 for ten lens/five source bins. Validate dimensions from metadata rather than hard-coding those examples. Never duplicate symmetric observables in a likelihood vector.

Keep complete input spectra for covariance products; selection happens afterward on the vector and BOTH matrix axes. Implement/save TT-autos, EE-unique and the adopted Niko/binny TE selection: source peak strictly above lens peak and integral min(p_lens,p_source) dz <= 0.10 (Y1) or <= 0.25 (Y10), using individually unit-normalized distributions on a declared common grid. Record peak ties, normalization, integration, thresholds, original distribution hashes, actual overlap values and actual accepted pair list. Do not infer eligibility from bin numbers or rename the rule as uniquely SRD-defined.

Use the explicit scale-cut policy fixed in the accepted run contract, with k units, reference cosmology/redshift, selected whole-band criterion and actual ell limits persisted. Check h/Mpc versus Mpc^-1 numerically. Apply a whole-bin upper-edge criterion when preserving the existing 20 uniform bands; do not truncate bins silently. A representative-redshift cutoff is not a sharp cutoff throughout the radial kernel. If a policy field remains unspecified, implement the configurable machinery and report that selected-vector acceptance is pending rather than fabricate a final mask. All-pair diagnostics can still finish.

For the adopted complete covariance and Gaussian reference, check full and selected finite square shape, positive diagonal, symmetry, labels/order, correlation bounds, component sum and eigenvalues/conditioning; require Cholesky and a deterministic solve residual on the nonredundant vectors used for inversion. Record tolerances and original asymmetry before diagnostic symmetrization. A connected NG correction is not independently required to be positive semidefinite: validate its signed physical kernel, windows, symmetry and convergence, and its contribution to the accepted total. Apply a separate SSC PSD expectation only when justified by its response-covariance model. Include a synthetic signed correction with an accepted positive-definite total to catch overstrict component validation. Test a known permutation and principal-submatrix mask. No jitter/eigenvalue clipping/nearest-SPD repair to hide a physics or serialization defect. Positive definiteness alone does not certify a physically correct covariance.

5. Run bounded allocated acceptance pilots and estimate production cost.

Create an explicit pilot manifest covering both surveys and all seven method/device/order identities: CCL, NUMBA, JAX CPU, JAX GPU, NUMERIC linear/quadratic/cubic. Cover actual Single=EE, Double=TE+TT, Triple=EE+TE+TT entry points: 42 workload identities. Do not multiply that count by CPU-allocation variants. The Slurm CPU count is runtime parallelism recorded on the job, not a separate workload and not a filename token. Start with validated tiny-run controls, e.g. fiducial plus two shared sampled IDs; prove N=0 evaluates sample 0 once and N>0 evaluates exactly IDs 0..N before execution. A coarse radial grid must still be mathematically valid for every interpolant. Smoke dispatch does not replace scientific acceptance at adopted resolution.

Use compute-node job steps with the actual approved account/resources; no expensive login-node execution. Confirm interpreter/check-out/device, JAX float64 and actual GPU placement, CPU thread pools/binding, HDF5 runtime path and recorded environment. Warm-up/compile separately; synchronize JAX correctly. Record compute stages, transfer/angular-estimator boundary, I/O and job wall time separately, peak host/device memory, shapes and sample IDs. Exercise ordinary fresh driver invocations and interruption/recovery through the transaction API/tooling, including mismatch rejection and preservation of accepted generations. Keep the timing drivers' simplified flags and canonical output locations. A resumed timing sum is segmented evidence. Compare all methods/orders on matched 20-band spectra and signed component checks; NUMERIC representation differences are not the same-integrand endpoint reference. Confirm selected representative nonfiducial and Y10 results, with declared reference tolerances and bounded resolution-sensitivity checks.

Run accepted fiducial Gaussian covariance for Y1/Y10 and tiny statistics/vector integration checks. Save actual code-test versus science-test results separately. Estimate each production workload's wall time, memory, Slurm resources, checkpoint overhead, cold cost and uncertainty from measured pilots; aggregate the 42 workloads. Extra CPU counts are not extra campaign dimensions. Identify required wall-time segmentation and continuous benchmark runs. Prepare deterministic Phase 4 launch commands/manifests but do not start the 1,000-sample campaign in this stage.

6. Pilot the separate fixed-cosmology and distribution-update benchmark.

Implement a bounded reusable-state benchmark, for example experiments/benchmarks/nuisance_updates.py, because manuscript MA43 requires evidence beyond varying cosmology. Cover Y1/Y10 Triple for CCL, NUMBA, JAX CPU and JAX GPU at the accepted radial grid and common 21-to-20 estimator. This is a separate fixture/repetition table, not another 1,000-cosmology ensemble or an expansion of the 42-workload matrix. NUMERIC may be included as a labelled numerical comparator when useful; it must not fabricate an analytical coefficient/contraction stage.

Freeze actual fiducial cosmology/power/nuisance functions and a small predetermined set of changed source/lens distributions before timing. Use baseline plus separate positive/negative source and lens shifts, for example +/-0.01 in z, sampled on the unchanged declared grids with explicit outside-support fill and normalization. Record the arrays, shift/normalization convention and hashes; these are timing fixtures, not an inferred photo-z prior. Confirm finite valid distributions and observable changes, and compare every final probe against an ordinary uncached evaluation on the same fixture. Do not compare different physics or ignore rebuilding q-weighted and IA distributions.

Measure warm coefficient+contraction with supplied cosmology/power, isolated fixed-basis contraction with prepared nodal weights, and complete fixed-cosmology distribution-update work including weight/tracer rebuild, final component assembly, angular estimator and declared materialization. Record preparation/cold costs separately. The efficient CCL comparator reuses cosmology/power and unaffected tracers while rebuilding affected tracers; it must not return cached spectra for changed inputs. Only compare equal completed outputs and cache boundaries; never promote isolated contraction speedup to end-to-end speedup. Synchronize JAX and charge all lazy/recompile work occurring within a timed call. Keep fixture I/O outside compute timers.

Use bounded repetitions in pilots; freeze the production repetition count from measured cost and precision before Phase 4. Record individual repetitions and their order, cache state/identity, hardware, warmup, median and spread, and output-agreement tests. The Phase 4 manifest must list this small benchmark separately, including its cost and required evidence. Missing GPU allocation or invalid cache reuse leaves the corresponding claim unaccepted.

7. Report independent readiness gates and the phase handoff.

Record spectra_production_ready from accepted physics, endpoint/NUMERIC/estimator tests, execution/artifact tests and costed allocated pilots. Record selected_gaussian_analysis_ready only when Gaussian model/windows/noise, vector/selection and solve checks pass. Record onecovariance_adapter_verified from the actual installed-reader/adapter evidence, and full_covariance_analysis_ready from the required matched NG/SSC components and accepted total. List the actual covariance backend, requested/accepted components and claims supported by each gate. Preserve an overall partial status while any required gate remains pending; no fallback silently removes a requested component or certifies upstream behavior.

An explicitly invoked Phase 4 may run spectra when spectra_production_ready passes, and selected Gaussian analysis when its gate passes, even if the upstream-specific or full-covariance gate is pending. The permitted project-Gaussian route must have an independent reference and truthful backend identity. Missing OneCovariance evidence blocks OneCovariance claims; missing NG/SSC evidence blocks full-covariance claims. Missing selection blocks selected statistics but not valid all-pair spectra/diagnostics. This defines dependencies, not automatic phase advancement or authorization for a new campaign.

The handoff must include tested source changes; installed-reader evidence or its exact outstanding gate; window/mean/noise and selection reports; tiny CPU/GPU/NUMERIC and supplied-state/distribution-update pilots; covariance/artifact identities; the costed workload inventory; start/end working-state identity and phase-only diff; commands, failures and remaining limitations. Update documentation to explain these contracts. Retain failed diagnostics. Do not claim an unrun allocation, absent NG/SSC validation, or incomplete vector is accepted. Leave the reviewable diff and report without automatic commit/push.
```
