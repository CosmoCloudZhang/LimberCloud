# Phase 5 — Remote release verification and publication-product handoff

Run this prompt in remote Cursor on NERSC after the Phase 4 campaign and summaries have been reviewed. This phase verifies the final code and evidence, closes bounded documentation and maintenance work, and prepares the local manuscript owner's handoff. It does not edit the manuscript or authorize external publication.

```text
Complete the remote release and handoff stage in the existing LimberCloud checkout. Read the current CODE_REVISION_PLAN.md, MANUSCRIPT_REVISION_PLAN.md, COAUTHOR_COMMENT_INVENTORY.md, the phase roadmap and accepted Phase 1–4 reports, plus the original C00 inventory/environment reports and their follow-up corrections. Use the current explicit scientific decisions; historical reports are evidence of earlier states, not instructions to restore superseded behavior.

Inspect the branch, revision, working tree, active interpreter, environment/module identity and external runtime paths before changes. Preserve unrelated work. Use the existing intended branch, or a codex/ phase branch if separately requested; do not imply that a specific branch name was already requested. Do not reset or rewrite history, initialize the manuscript, automatically commit, or push. Return a reviewable diff and report; a later explicit commit/push request is a separate action. Do routine authorized edits and checks without repeated confirmation requests.

Preserve entry/exit source snapshots/manifests and an incremental phase diff under the master review-packet contract. HEAD alone cannot separate consecutive uncommitted stages. Keep reports and execution provenance separate from compute-source fingerprints.

1. Establish the accepted release inputs.

Collect the accepted configuration, canonical cosmology table, evaluator/schema versions, covariance and selection identities, campaign manifests and validation reports. Verify that the 42 requested workloads are accounted for: seven method/device/order choices (CCL, NUMBA, JAX CPU, JAX GPU, NUMERIC LINEAR, NUMERIC QUADRATIC and NUMERIC CUBIC), two surveys and three configurations. NUMERIC orders apply only to NUMERIC. Single=EE, Double=TE+TT and Triple=EE+TE+TT.

For each workload record its attempted, completed, failed and matched sample IDs; the requested full campaign is fiducial ID 0 plus the common sampled IDs 1–1000. Validate identity and completion from manifests and files, not filenames or the existence of a directory. Reconcile missing or failed work without silently dropping samples, substituting random draws or calling a partial run complete. Report whether timing evidence is uninterrupted or segmented; do not promote segmented accumulated compute time into an uninterrupted benchmark claim. Check that fiducial/cold costs and output I/O remain outside sampled cumulative compute totals.

The accepted science contract includes eta_IA=0, the reviewed endpoint treatment, common 21 multipole samples and the shared natural-cubic angular operator producing 20 bandpowers, and declared independent NUMERIC radial interpolants. Verify each producing run used the accepted contract. A changed scientific identity requires rerunning affected evidence through its earlier gate; editing a release manifest cannot make an old result compatible. Do not undertake a new grid optimization or repeat the whole campaign merely for release packaging.

2. Preserve the code/paper boundary and finish bounded maintenance.

Verify the reviewed mode-160000 paper pin with `git ls-files --stage manuscript` and the staged diff, and separately report `git ls-tree HEAD manuscript`. If commits were not authorized, a correct reviewed index repair may precede its appearance in HEAD; the final authorized parent commit must include it. Verify the matching .gitmodules entry. The prior accidental deletion must already have been repaired. Preserve that repair. An absent/deinitialized remote paper directory is intentional and must never be staged as deleting its gitlink. Do not run git -C manuscript or create a replacement paper directory. Source, figures, tables, captions, manuscript manifests and compilation belong to the local paper owner.

Check the earlier environment/launcher cleanup against actual current behavior. Reuse the accepted dedicated environment; do not recreate it or modify preserved CosmoConda. Confirm that .venv selects the documented prefix, batch and supported kernel launches receive its required activation/module state, runtime roots remain external, launcher flags reach the actual runner, and empty/rejected selections cannot overwrite previous timing products. Verify that covariance uses a demonstrated working upstream interpreter/dependency stack. Retain exact installed package/module/build identities and identify any drift from earlier smokes.

Apply only bounded release fixes: stale links, incorrect commands, obsolete selectors or --path references, misleading help, small portability issues and proven test-fixture defects. Keep scientific changes in separately identified patches with renewed affected acceptance. Do not mix broad reformatting or an unrequested refactor into an accepted numerical baseline. Update README and documents/ to describe implemented behavior and the current stage sequence, including shell requirements, isolated run IDs, restart ownership and the absent-paper NERSC workflow.

3. Run final verification with real evidence.

Run the complete documented make check suite, fixing actionable failures in scope. Report every component, invocation, host, interpreter and result; a skipped or unavailable check is pending, never passed. Split login-safe syntax/contract checks from allocation-dependent checks where required. In particular, an MPI-enabled h5py build can initialize MPI during HDF5 tests: run those and scientific/device checks inside a supported allocation. Local portable serial-HDF5 tests are a separate environment claim.

Confirm that environment and launcher tests are self-contained: temporary project roots and fake prefixes must not require the user's real .venv or initialized paper. Include copied Slurm scripts, argument forwarding, nested-paper root discovery, paths containing spaces and supported macOS Bash behavior where applicable. A Linux result alone does not certify macOS portability; retain exact local review evidence or mark that platform pending.

Build and inspect the source distribution and wheel in a temporary clean materialization of the accepted source snapshot with the manuscript uninitialized. Include the reviewed staged/unstaged/untracked source changes; a clean HEAD checkout alone may package the old implementation. Verify the materialized manifest and installed-package import provenance, then run ordinary checks in the intended installed configuration. Confirm package contents exclude manuscript/revision assets, runtime datasets, local configuration and secrets; verify required package resources are present. Building an archive alone is insufficient: inspect its members and exercise the installed entry path. Do not install or rebuild dependencies in an accepted environment simply to perform a packaging check; use an isolated test destination.

Execute the six spectra and six error notebooks in fresh supported kernels, plus both experiments/benchmarks/{Y1,Y10}/benchmark.py plotting scripts and their figure.sh launch paths, against accepted HDF5 products and summaries. Do not invent benchmark notebooks. Actually read representative/all required data slices and regenerate the publication outputs; JSON/AST checks and placeholder files do not establish this acceptance. Verify that plotting paths do not invoke expensive evaluators or import unnecessary scientific backends, read bounded slices, close handles/figures and write to the external runtime tree. Use an allocation where the HDF5 build requires it. Inspect axes, units, pair labels, Y1/Y10 selections, logarithmic zero/near-zero handling, fiducial exclusion from sampled quantiles and reported successful counts. Record failures and withhold affected outputs.

4. Prepare an immutable, reproducible CFS handoff.

Create a uniquely labelled CFS handoff directory containing accepted publication figures, compact tables/summaries and the manifest needed to interpret them. Keep full HDF5 ensembles, covariance matrices and coefficient diagnostics on CFS in their established run namespaces; do not copy them into Git or the manuscript bundle. Retain durable acceptance logs or referenced log copies on CFS, not solely PSCRATCH.

The handoff manifest must enumerate files and checksums, media/table roles, producing code commit and any exact dirty-patch identity, producer commands, input identities, run IDs, sample-table/configuration hashes, covariance and selected-vector hashes, estimator and schema versions, environment/build identity, completion/matched counts, timing semantics and acceptance status. Verify every exported file against its checksum after publication. Reference the external large products by stable CFS location and identity. Publish the completed bundle manifest last; incomplete exports must not look accepted.

Distinguish producing scientific code from later release/docs changes. Record the commit or explicit patch that actually computed each product, the plotting code that generated each figure, and the current release-review revision separately when they differ. Do not replace producing identities with the latest documentation commit. If source changes affect results, invalidate and regenerate only the dependent products through the appropriate acceptance gate. If a producing tree was dirty, preserve its exact patch/hash and explain the limitation instead of claiming a clean commit.

Provide short, tested reproduction instructions: obtain the reviewed code revision, select the existing supported environment or its recorded recipe, set private runtime paths, verify configuration/table hashes, validate manifests, read a tiny accepted sample selection, resume only within a matching owned namespace, and regenerate figures/tables. Separate preparation, compute and plotting commands. Use real flags and paths appropriate to this host; no invented launchers or commands that trigger a hidden full campaign. State how a new user obtains external inputs without bundling private or large data.

5. Return the review and local-owner handoff.

Write `revisions/2026-09/reports/PHASE_5_RELEASE_AND_HANDOFF_REPORT.md` and update the completion checklist with evidence and unresolved items.

Return changed paths and rationale, the source diff, verification results and unresolved items, current code identity, parent-recorded paper pin, producing identities, campaign/accounting matrix, and the absolute CFS handoff location plus manifest checksum. Review the report against source behavior and retained logs; distinguish independently observed evidence from earlier reported smokes. If some gate is pending, identify exactly which scientific figures or claims remain unsupported while completing unrelated release work.

Map accepted figures/tables and compact numerical summaries to the manuscript sections/coauthor requirements they support. This is an evidence handoff, not permission to change claims remotely. The local owner verifies transferred checksums, updates the independent paper repository and its provenance, reconciles captions/abstract/conclusion with accepted evidence, and compiles/visually inspects the paper. Do not assume direct local CFS access: prepare the bounded transferable bundle and document the established transfer method or missing transfer step. Do not send coauthor messages, synchronize Overleaf, publish a site, or perform unsolicited external publication. Leave commits and pushes for an explicit request after the source, reports and bundle are reviewable.
```
